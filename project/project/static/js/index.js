/* =============================================================
   Encore — Homepage
   Responsibilities: homepage-specific UI enhancements only.
   ============================================================= */

(() => {
  "use strict";

  /* Add homepage-specific interactions here as the project grows.
     Examples: hero parallax, dish hover effects, reservation form UX. */

  document.addEventListener("DOMContentLoaded", () => {
    /* Homepage ready */
  });
})();